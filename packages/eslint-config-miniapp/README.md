# eslint-config-miniapp

Eslint config for Mniapp

## Usage

1. `npm install --save-dev babel-eslint eslint-config-miniapp`
2. Create a file named .eslintrc in your project:

```js
{
  extends: 'miniapp'
}
```
