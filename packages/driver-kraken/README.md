# Drvier Kraken

Rax driver for kraken.

## Usage

```jsx
import { render, createElement } from 'rax';
import createDriver from 'driver-kraken';

render(<App />, null, {
  driver: createDriver(),
})
```
