# Drvier Kraken

Rax driver for kraken.

## Usage

```jsx
import { render, createElement } from 'rax';
import * as KrakenDriver from 'driver-kraken';

render(<App />, null, {
  driver: KrakenDriver,
});
```
