{
  "plugins": [
    <%_ if (projectFeatures.includes('ssr')) { -%>
      ["rax-plugin-app", { "targets": ["<%- projectTargets.join('", "') %>"]}],
      "rax-plugin-ssr",
    <%_ } else { -%>
      ["rax-plugin-app", { "targets": ["<%- projectTargets.join('", "') %>"]}]
    <%_ } -%>
  ]
}
