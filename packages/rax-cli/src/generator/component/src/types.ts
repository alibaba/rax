import * as Rax from 'rax';

export interface Props extends Rax.Attributes {
  /**
   * add Component Props Like
   * ```ts
   *  style?: Rax.CSSProperties;
   *  onClick?: () => void;
   * ```
   */

  style?: Rax.CSSProperties;
}
