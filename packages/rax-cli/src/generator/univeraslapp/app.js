/**
 * App entry.
 */
export default class App {
  /**
   * App config.
   */
  static config = {
    'pages': [
      'pages/home'
    ],
    'window': {
      'defaultTitle': 'MiniApp'
    }
  };

  onLaunch(options) {}
}
