const generate = require('../generate');
const modules = require('../transpileModules');

const transpilerOptions = { modules };

describe('Transpiler generate', () => {
  it('should use a:for with other rules', () => {

  });
});
