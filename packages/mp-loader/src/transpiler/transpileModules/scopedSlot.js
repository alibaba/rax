const { getAndRemoveAttr } = require('../helpers');
const expressionHelpers = require('../expression');

/**
 * Transpile scopedSlots.
 * @param {*} node
 */
function transformNode(node) {
}

function genData(el) {

}

module.exports = {
  transformNode,
  genData,
};
