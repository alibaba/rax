<scroll-view class="{{className}}" 
    style="{{style}}"
    scroll-x="{{direction=='horizontal'}}"
    scroll-y="{{direction=='vertical'}}"
    lower-threshold="{{endReachedThreshold}}"
    onScroll="onScroll"
    onScrollToLower="onEndReached" 
    >
    <slot/>
</scroll-view>