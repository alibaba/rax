{
  "private": true,
  "scripts": {
    "build": "rax-scripts build",
    "start": "rax-scripts start",
    "link": "./link.sh",
    "reset": "rm -rf node_modules && npm i",
    "setup": "npm run reset && npm run link"
  },
  "dependencies": {
    "driver-universal": "^1.0.1",
    "history": "^4.9.0",
    "rax": "^1.0.8",
    "universal-app-runtime": "^0.1.1"
  },
  "devDependencies": {
    "rax-scripts": "^1.3.11",
    "rax-plugin-app": "^0.1.0-0"
  }
}
