{
  "private": true,
  "scripts": {
    "start": "http-server",
    "deploy": "surge --project ./ --domain rax.surge.sh",
    "deploy-fed": "surge --project ./ --domain rax.taobaofed.org"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "surge": "latest"
  }
}
