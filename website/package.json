{
  "private": true,
  "scripts": {
    "start": "http-server",
    "deploy": "surge --project ./ --domain rx.surge.sh"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "surge": "latest"
  }
}
